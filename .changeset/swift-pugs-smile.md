---
'lily-svelte': patch
---

Point the registry base URL at the new Workers deployment. The docs site moved from Cloudflare Pages (`lily-svelte.pages.dev`) to Workers (`lily-svelte.levish.workers.dev`), and the old origin is going away.
